1.树是n(n>=0)个结点的有限集
  n=0时为空树
  在任意一棵非空树中应满足
  (1)有且仅有一个结点称为根的根的结点
  (2)其余结点可分为m(m>0)个不互相交的有限集
     其中每个集合本身又是一棵树,并且称为根的子树
  (3)树是一种递归的数据结构
     1)树的根结点没有前驱,除根结点外所有结点只有一个前驱
     2)树中所有结点可以有零个或多根后继
2.树适合表示具有层次结构的数据,树种的某个结点(除根结点外)最多只和上一层
  的一个结点(即其父结点)有直接关系,根结点没有直接上层结点,因此n各结点的树有n-1条边
3.树种每个结点与其下一层的零个或多个结点(即其子女结点)有直接关系
4.从根结点到目标结点的唯一路径上的任何点称为该点的祖先,该路径上任何点是该点的祖先
5.路径上最接近该点的结点称为该点的双亲,根结点是树中唯一没有双亲的结点
6.有相同双亲的结点称为兄弟,双亲在同一层的结点互为堂兄弟
7.一个结点的孩子个数称为该结点的度,树中结点最大度数为树的度
8.度大于0的结点称为分支结点(又称为非终端结点),度为0(没有子女结点)的结点称为叶子结点(又称终端结点)
  每个结点的分支数就是该结点的度
9.结点的层次从树根开始,根结点为第1层,它的子结点为第2层,以此类推
  结点的深度是从根结点开始自顶向下逐层累加的
  结点的高度是从叶结点开始自底向上逐层累加的
10.树中结点的各子树从左到右是有次序的,称为有序树,否则称为无序树
11.树种两个结点之间的路径是由这两个结点之间所经过的结点序列构成的,路径长度是路径上所经过的边的个数
12.森林是m棵互不相交的树,把树的根结点去了就是森林,m棵独立的树加上一个结点森林就变成了树
13.树中的结点数等于所有结点的度数加1
14.度数为m的树种第i层至多有m^(i-1)个结点
15.高度为h的m叉树至多有(m^h-1)/(m-1)个结点
   1+m+m^2+....m^(h-1)=(m^h-1)/(m-1)
16.具有n个结点的m叉树的最小高度为[logm (n(m-1)+1)]


1.D
2.A
3.C
4.A
5.A
6.C
7.B

1.n-2
2.?   n=n0+n1+n2+n3+n4+1    n=1n1+2n2+3n3+4n4 可以建立方程组求解
3.?   叶子结点就是n0   n0=1n1+2n2+3n3+4n4+1-n1-n2-n3-n4
